# Installation

Install dependencies(ignore if using replit):
```
python -m venv .venv
pyenv shell
pip install -r requirements.txt
```

Create Secrets file:
```
mkdir bot_config
cd bot_config
touch secrets.json
```
